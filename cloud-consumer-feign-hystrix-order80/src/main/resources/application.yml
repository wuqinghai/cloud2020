server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
#使用openfeign进行接口调用的时候，开启hystrix
feign:
  hystrix:
    enabled: true